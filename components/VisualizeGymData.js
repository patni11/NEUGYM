import { Chart } from "chart.js";
import { MatrixController, MatrixElement } from "chartjs-chart-matrix";

Chart.register(MatrixController, MatrixElement);
const VisualizeGymData = ({ title, data }) => {
  return (
    <div>
      <h3>title</h3>
    </div>
  );
};
